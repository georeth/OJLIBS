split arith/gcd/crt/factorize
binary search shorthand
	first false of f(x, ctx) => lower_bound(range, ctx, f)
	first true  of f(x, ctx) => lower_bound(range, ctx, !f)

	use +-1 trick for integer sequence
rethink segtree (444C)
Edmond's algorithm to find maximum matching (generic graph) https://en.wikipedia.org/wiki/Blossom_algorithm
ac_automaton: use add, build interfaces. it should behave like a map from string to answer. (ac_automaton.hpp -> aca.hpp?)

Now we have a new graph template. should Dijkstra, Dinic, 2SAT, MCMF be is-a, has-a, or another implementation?
